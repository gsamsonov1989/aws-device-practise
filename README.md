It is assumed that serverless has been initialized in the current directory. If not
then run 
`serverless` command to init 

Example of build/deploy

`export SERVERLESS_ACCESS_KEY=<key> && sh build_deploy.sh`